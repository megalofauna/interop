# Mental-model cards and workflow docs.
#
# Prettier's markdown printer repads every table to full width and reindents
# code fences, which turns a one-line addition to a card into a whole-file
# rewrite and leaves it out of step with its siblings. These are documents
# written for humans to diff, not source.
.agent/

# Build output.
dist/

# Generated stylesheets. The generator is the formatter — running prettier over
# these makes `--check` fail against a freshly generated file for cosmetic
# reasons, which turns a drift guard into a nuisance.
projects/interop/src/lib/styles/interop.starter.css
projects/interop/src/lib/styles/themes/protocol/ladder.css
projects/interop/src/lib/styles/tokens/elevation.css
projects/interop/src/lib/styles/interop.tokens.css
projects/interop/src/lib/styles/tokens/ladder.css-source.ts
projects/demo/src/app/pages/color/ladder-facts.ts
projects/demo/src/app/pages/color/palette-preview.ts

# Generated icon components — 18,431 files, ~99% of the repo's file count and
# every one of them emitted by scripts/generate-icons.mjs. Formatting them would
# produce an 18k-file diff that the next `npm run build:icons` silently undoes.
# The generator's output template is the formatter here.
projects/interop/src/lib/iconsets/

# npm owns this file's format; prettier and npm would fight over it on every install.
package-lock.json
