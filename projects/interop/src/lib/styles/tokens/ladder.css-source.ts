/*
 * The colour stylesheets as strings — GENERATED by
 * scripts/inline-css-fixture.mjs. Do not edit by hand; edit the CSS.
 *
 * Specs run in a browser and cannot read files, so this is how they get at
 * the real stylesheet rather than a replica that could drift from it.
 */

export const LADDER_CSS = "/*\n * Colour ladder — the palette. Hand-authored; edit it directly.\n *\n * Three things live here and nothing else: six surface VALUES, the ROLES drawn\n * on them, and one hue number per accent family.\n *\n * A family is that hue and nothing more. At fixed lightness and chroma,\n * contrast barely moves around the hue circle — chroma is the variable that\n * breaks it, and --itx-accent-chroma caps it where every hue clears its floor\n * without a per-hue correction.\n *\n * Every floor is measured rather than asserted. scripts/check-contrast-css.mjs\n * resolves each role through the real cascade at every depth the engine can\n * reach; scripts/check-contrast-render.mjs paints the literals in Chrome and\n * reads the pixels back. Move a value and they will tell you.\n *\n * Both schemes live in one light-dark() per token. That is not a style choice:\n * color-scheme is `light dark` on [interop-root], and while it computes to\n * that, nothing in CSS can observe which scheme is actually in use — not\n * @supports, not a style query, not @container. Only light-dark() knows. So\n * scheme-varying values have to ride inside a colour function.\n *\n * Roles are declared in EVERY block that redeclares a hue. A role written once\n * at [interop-root] would compute there and inherit as a finished colour, so a\n * colourway or status-palette variant would keep the root's roles and the swap\n * would be visibly correct in the stylesheet and inert on screen.\n *\n * The surfaces are the exception, and the block below explains why: they are\n * declared at the root alone so a consumer can override them.\n * tokens/elevation.css holds no colour — it counts depth and indexes these\n * four.\n */\n\n/*\n * ── Elevation: the neutral substrate ─────────────────────────────────────\n *\n * Four values, 5% apart, and the number IS the depth. tokens/elevation.css\n * indexes them: layer 0 is surface-0, layer 1 is surface-1, layer 2 is\n * surface-2, and surface-3 is what --itx-surface-above resolves to at the\n * deepest layer.\n *\n * There is one page and it is layer 0. Nothing sits below it.\n *\n * Four is what the text tiers can carry, and it is measured rather than\n * chosen. A control fill sits 0.05 out from the surface it lands on, so the\n * deepest painted background is surface-2 plus a fill — which is surface-3\n * exactly. One more value at this spacing would be 0.41 in dark, and at 0.10\n * out from the surface three of the four tiers stop clearing.\n *\n * Declared at the root ONLY, which is the opposite of the rule the roles\n * follow, and deliberately. Repeating them on [itx-layer] would recompose the\n * tint at each layer — but a declaration beats inheritance, so every layer\n * would also stomp whatever a consumer set above it, and the four values would\n * stop being overridable at all. Overriding is worth more than retinting\n * mid-tree, and it costs nothing here: retinting a subtree is redeclaring\n * these four on it, the same act as retuning the ramp.\n *\n * Both are ordinary CSS and need no dials. --itx-tint-* is the shorthand that\n * keeps chroma and hue in one place across all four.\n */\n:where([interop-root]) {\n\t--itx-surface-0: light-dark(\n\t\toklch(0.96 var(--itx-tint-light)),\n\t\toklch(0.21 var(--itx-tint-dark))\n\t);\n\t--itx-surface-1: light-dark(\n\t\toklch(0.91 var(--itx-tint-light)),\n\t\toklch(0.26 var(--itx-tint-dark))\n\t);\n\t--itx-surface-2: light-dark(\n\t\toklch(0.86 var(--itx-tint-light)),\n\t\toklch(0.31 var(--itx-tint-dark))\n\t);\n\t--itx-surface-3: light-dark(\n\t\toklch(0.81 var(--itx-tint-light)),\n\t\toklch(0.36 var(--itx-tint-dark))\n\t);\n}\n\n\n/*\n * ── Roles — the named jobs ───────────────────────────────────────────────\n *\n * namespace · type · role · family · modifier. Role is the primary\n * distinction: this is a text thing, an edge thing. Family names the ramp,\n * modifier adjusts the base, and both trailing segments are optional — which\n * is why the common case is the shortest to write.\n *\n * One grammar in both slots: unmarked is full strength, a modifier is quieter.\n *\n * The text tiers are solved against the deepest surface WITH an interactive\n * fill on it, which is the real worst case — a hovered row at the deepest\n * nesting. Each arm is solved from its own floor rather than mirrored, because\n * contrast does not mirror: the two arms land on the same ratios from\n * completely different lightnesses.\n *\n *   text  the far end   quiet  7:1   quieter  4.5:1   disabled  3:1\n *\n * Named for how much they stand out, not for how light they are. A brightness\n * word is true in one arm and false in the other — the dark ramp runs 0.664 to\n * 0.970 while the light ramp runs 0.489 to 0.150.\n */\n:where([interop-root]) {\n\t--itx-role-text: light-dark(\n\t\toklch(0.15 var(--itx-tint-light)),\n\t\toklch(0.97 var(--itx-tint-dark))\n\t);\n\t--itx-role-text-quiet: light-dark(\n\t\toklch(0.285 var(--itx-tint-light)),\n\t\toklch(0.901 var(--itx-tint-dark))\n\t);\n\t--itx-role-text-quieter: light-dark(\n\t\toklch(0.398 var(--itx-tint-light)),\n\t\toklch(0.771 var(--itx-tint-dark))\n\t);\n\t/* WCAG exempts a disabled control from any floor, so 3:1 is a house\n\t   choice — enough to read, plainly quieter than live text. */\n\t--itx-role-text-disabled: light-dark(\n\t\toklch(0.489 var(--itx-tint-light)),\n\t\toklch(0.664 var(--itx-tint-dark))\n\t);\n\t/* One label for every family fill, worst 4.75 on info. The fill is\n\t   scheme-invariant, so its label is too. */\n\t--itx-role-text-inverse: oklch(0.97 var(--itx-tint-light));\n\n\t/*\n\t * Two weights of one ink, so they hold as the surface climbs. The\n\t * separator varies about 0.2 across all twelve backgrounds — six surfaces,\n\t * bare and filled — where an opaque border drifts 0.58 across three layers.\n\t *\n\t * Opacity is per-scheme because contrast is not symmetric: the same 25%\n\t * reads about 2.15 on dark and 1.66 on light. It has to be baked into the\n\t * colour rather than carried as a number — light-dark() takes <color> only,\n\t * so light-dark(33%, 25%) silently resolves the whole color-mix to\n\t * transparent, which measures 1.00 and looks like a border that is not there.\n\t *\n\t * The edge clears 3:1 on every one of those backgrounds, worst 3.10. The\n\t * divider has no floor: its job is to not assert itself.\n\t */\n\t--itx-role-edge: light-dark(\n\t\tcolor-mix(in srgb, var(--itx-role-text) 50%, transparent),\n\t\tcolor-mix(in srgb, var(--itx-role-text) 44%, transparent)\n\t);\n\t--itx-role-divider: light-dark(\n\t\tcolor-mix(in srgb, var(--itx-role-text) 33%, transparent),\n\t\tcolor-mix(in srgb, var(--itx-role-text) 25%, transparent)\n\t);\n\n\t/*\n\t * The only job the palette cannot express, because it works over arbitrary\n\t * content rather than over a known surface. Scheme-invariant. Its job is\n\t * dimming — body text on the page drops from 13.18 to 3.92 in dark and\n\t * 12.82 to 4.19 in light — not separating the dialog from the page, which\n\t * in dark mode comes from the dialog's own surface and its shadow.\n\t */\n\t--itx-role-scrim: oklch(0 0 0 / 0.5);\n\n\t/*\n\t * The chroma cap, and the whole reason a family is one hue number. At fixed\n\t * lightness and chroma, contrast barely moves around the hue circle; chroma\n\t * is the variable that breaks it. Spread across five hues at L 0.60: 0.06 at\n\t * chroma 0.02, 0.42 at 0.105, 1.51 at 0.22. Past 0.18 the per-hue correction\n\t * the deleted solver used to compute comes back.\n\t */\n\t--itx-accent-chroma: 0.16;\n\t--itx-accent-chroma-subtle: 0.064;\n}\n\n/*\n * The interactive fill — hover and selected, which share it and do NOT\n * compound. Derived from whatever surface it lands on, half a layer step out.\n *\n * Compounding is what breaks the ramp: at +0.075 the room between the 7:1 tier\n * and the far end collapses to 0.004 and the top two text tiers stop\n * separating. The difference between hover and selected is carried by a mark,\n * which needs only 3:1 against the surrounding surface — WCAG 1.4.11 asks for\n * 3:1 against ADJACENT colours, and a component's two states are never\n * adjacent. They are the same pixels at different times.\n *\n * Declared on every element the engine writes --itx-surface on, because it\n * composes where it is declared. One declaration at the root would compose\n * there and inherit as a finished colour, so a deeper layer would keep the\n * root's fill.\n */\n:where([interop-root], [itx-layer], [itx-sink]) {\n\t--itx-role-background-interactive: light-dark(\n\t\toklch(from var(--itx-surface) calc(l - 0.025) c h),\n\t\toklch(from var(--itx-surface) calc(l + 0.025) c h)\n\t);\n\n\t/*\n\t * A filled control's own plane — button, track, thumb, tab, step indicator,\n\t * list marker. A full step where the interactive fill is half, and permanent\n\t * where that one is a state.\n\t *\n\t * Text clears every tier on it at every depth the engine can reach: 9.69 for\n\t * body, 7.86 quiet, 5.09 quieter, 3.44 disabled, worst arm. There is room\n\t * above it too — a control that hovers derives from its OWN rest fill rather\n\t * than from the surface, so an override carries into the state, and lands at\n\t * a step and a half out, which still clears 4.67 at the 4.5 tier.\n\t */\n\t--itx-role-background-control: light-dark(\n\t\toklch(from var(--itx-surface) calc(l - 0.05) c h),\n\t\toklch(from var(--itx-surface) calc(l + 0.05) c h)\n\t);\n}\n\n:where([interop-root]) {\n\t/* ── Tint packs — chroma + hue, per scheme. The retint knob. ────── */\n\t--itx-tint-light: 0.006 250;\n\t--itx-tint-dark: 0.006 250;\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\t/*\n\t * Colourway.\n\t *\n\t * Seeded with ONE colour. Hue carries through; chroma is an intent\n\t * clamped to what the hue can actually reach; lightness is honoured for\n\t * the solid where the gamut allows and solved from contrast targets for\n\t * everything else. That is why this survives a hue change and a\n\t * lightness-indexed slot ramp did not.\n\t *\n\t * solid and on-solid are scheme-invariant on purpose: a brand colour\n\t * that shifts between light and dark is not an identity.\n\t */\n\t--itx-colorway-hue: 264;\n\t--itx-role-text-colorway: light-dark(\n\t\toklch(0.364 var(--itx-accent-chroma) var(--itx-colorway-hue)),\n\t\toklch(0.811 var(--itx-accent-chroma) var(--itx-colorway-hue))\n\t);\n\t--itx-role-edge-colorway: light-dark(\n\t\toklch(0.46 var(--itx-accent-chroma) var(--itx-colorway-hue)),\n\t\toklch(0.68 var(--itx-accent-chroma) var(--itx-colorway-hue))\n\t);\n\t--itx-role-background-colorway: oklch(0.5 var(--itx-accent-chroma) var(--itx-colorway-hue));\n\t--itx-role-background-colorway-subtle: light-dark(\n\t\toklch(0.89 var(--itx-accent-chroma-subtle) var(--itx-colorway-hue)),\n\t\toklch(0.28 var(--itx-accent-chroma-subtle) var(--itx-colorway-hue))\n\t);\n}\n\n\n/*\n * ── Status families ─────────────────────────────────────────────────────\n *\n * Same solver, four more seeds per palette. Solved against layer 0 rather\n * than per layer — see perLayer() in the generator for why.\n *\n * The labels are derived, which retires two recorded defects: warning used\n * to sit at 4.71:1 with 'neither much margin', and the eighties palette used\n * flat on-* values, the exact pattern seventies documents as a dark-mode AA\n * failure it already had to fix.\n */\n:where([interop-root], [itx-status-palette=\"seventies\"]) {\n\t--itx-danger-hue: 33;\n\t--itx-info-hue: 218;\n\t--itx-success-hue: 122;\n\t--itx-warning-hue: 78;\n\t--itx-role-text-danger: light-dark(\n\t\toklch(0.364 var(--itx-accent-chroma) var(--itx-danger-hue)),\n\t\toklch(0.811 var(--itx-accent-chroma) var(--itx-danger-hue))\n\t);\n\t--itx-role-edge-danger: light-dark(\n\t\toklch(0.46 var(--itx-accent-chroma) var(--itx-danger-hue)),\n\t\toklch(0.68 var(--itx-accent-chroma) var(--itx-danger-hue))\n\t);\n\t--itx-role-background-danger: oklch(0.5 var(--itx-accent-chroma) var(--itx-danger-hue));\n\t--itx-role-background-danger-subtle: light-dark(\n\t\toklch(0.89 var(--itx-accent-chroma-subtle) var(--itx-danger-hue)),\n\t\toklch(0.28 var(--itx-accent-chroma-subtle) var(--itx-danger-hue))\n\t);\n\t--itx-role-text-info: light-dark(\n\t\toklch(0.364 var(--itx-accent-chroma) var(--itx-info-hue)),\n\t\toklch(0.811 var(--itx-accent-chroma) var(--itx-info-hue))\n\t);\n\t--itx-role-edge-info: light-dark(\n\t\toklch(0.46 var(--itx-accent-chroma) var(--itx-info-hue)),\n\t\toklch(0.68 var(--itx-accent-chroma) var(--itx-info-hue))\n\t);\n\t--itx-role-background-info: oklch(0.5 var(--itx-accent-chroma) var(--itx-info-hue));\n\t--itx-role-background-info-subtle: light-dark(\n\t\toklch(0.89 var(--itx-accent-chroma-subtle) var(--itx-info-hue)),\n\t\toklch(0.28 var(--itx-accent-chroma-subtle) var(--itx-info-hue))\n\t);\n\t--itx-role-text-success: light-dark(\n\t\toklch(0.364 var(--itx-accent-chroma) var(--itx-success-hue)),\n\t\toklch(0.811 var(--itx-accent-chroma) var(--itx-success-hue))\n\t);\n\t--itx-role-edge-success: light-dark(\n\t\toklch(0.46 var(--itx-accent-chroma) var(--itx-success-hue)),\n\t\toklch(0.68 var(--itx-accent-chroma) var(--itx-success-hue))\n\t);\n\t--itx-role-background-success: oklch(0.5 var(--itx-accent-chroma) var(--itx-success-hue));\n\t--itx-role-background-success-subtle: light-dark(\n\t\toklch(0.89 var(--itx-accent-chroma-subtle) var(--itx-success-hue)),\n\t\toklch(0.28 var(--itx-accent-chroma-subtle) var(--itx-success-hue))\n\t);\n\t--itx-role-text-warning: light-dark(\n\t\toklch(0.364 var(--itx-accent-chroma) var(--itx-warning-hue)),\n\t\toklch(0.811 var(--itx-accent-chroma) var(--itx-warning-hue))\n\t);\n\t--itx-role-edge-warning: light-dark(\n\t\toklch(0.46 var(--itx-accent-chroma) var(--itx-warning-hue)),\n\t\toklch(0.68 var(--itx-accent-chroma) var(--itx-warning-hue))\n\t);\n\t--itx-role-background-warning: oklch(0.5 var(--itx-accent-chroma) var(--itx-warning-hue));\n\t--itx-role-background-warning-subtle: light-dark(\n\t\toklch(0.89 var(--itx-accent-chroma-subtle) var(--itx-warning-hue)),\n\t\toklch(0.28 var(--itx-accent-chroma-subtle) var(--itx-warning-hue))\n\t);\n}\n\n:where([itx-status-palette=\"eighties\"]) {\n\t--itx-danger-hue: 25;\n\t--itx-info-hue: 264;\n\t--itx-success-hue: 145;\n\t--itx-warning-hue: 85;\n\t--itx-role-text-danger: light-dark(\n\t\toklch(0.364 var(--itx-accent-chroma) var(--itx-danger-hue)),\n\t\toklch(0.811 var(--itx-accent-chroma) var(--itx-danger-hue))\n\t);\n\t--itx-role-edge-danger: light-dark(\n\t\toklch(0.46 var(--itx-accent-chroma) var(--itx-danger-hue)),\n\t\toklch(0.68 var(--itx-accent-chroma) var(--itx-danger-hue))\n\t);\n\t--itx-role-background-danger: oklch(0.5 var(--itx-accent-chroma) var(--itx-danger-hue));\n\t--itx-role-background-danger-subtle: light-dark(\n\t\toklch(0.89 var(--itx-accent-chroma-subtle) var(--itx-danger-hue)),\n\t\toklch(0.28 var(--itx-accent-chroma-subtle) var(--itx-danger-hue))\n\t);\n\t--itx-role-text-info: light-dark(\n\t\toklch(0.364 var(--itx-accent-chroma) var(--itx-info-hue)),\n\t\toklch(0.811 var(--itx-accent-chroma) var(--itx-info-hue))\n\t);\n\t--itx-role-edge-info: light-dark(\n\t\toklch(0.46 var(--itx-accent-chroma) var(--itx-info-hue)),\n\t\toklch(0.68 var(--itx-accent-chroma) var(--itx-info-hue))\n\t);\n\t--itx-role-background-info: oklch(0.5 var(--itx-accent-chroma) var(--itx-info-hue));\n\t--itx-role-background-info-subtle: light-dark(\n\t\toklch(0.89 var(--itx-accent-chroma-subtle) var(--itx-info-hue)),\n\t\toklch(0.28 var(--itx-accent-chroma-subtle) var(--itx-info-hue))\n\t);\n\t--itx-role-text-success: light-dark(\n\t\toklch(0.364 var(--itx-accent-chroma) var(--itx-success-hue)),\n\t\toklch(0.811 var(--itx-accent-chroma) var(--itx-success-hue))\n\t);\n\t--itx-role-edge-success: light-dark(\n\t\toklch(0.46 var(--itx-accent-chroma) var(--itx-success-hue)),\n\t\toklch(0.68 var(--itx-accent-chroma) var(--itx-success-hue))\n\t);\n\t--itx-role-background-success: oklch(0.5 var(--itx-accent-chroma) var(--itx-success-hue));\n\t--itx-role-background-success-subtle: light-dark(\n\t\toklch(0.89 var(--itx-accent-chroma-subtle) var(--itx-success-hue)),\n\t\toklch(0.28 var(--itx-accent-chroma-subtle) var(--itx-success-hue))\n\t);\n\t--itx-role-text-warning: light-dark(\n\t\toklch(0.364 var(--itx-accent-chroma) var(--itx-warning-hue)),\n\t\toklch(0.811 var(--itx-accent-chroma) var(--itx-warning-hue))\n\t);\n\t--itx-role-edge-warning: light-dark(\n\t\toklch(0.46 var(--itx-accent-chroma) var(--itx-warning-hue)),\n\t\toklch(0.68 var(--itx-accent-chroma) var(--itx-warning-hue))\n\t);\n\t--itx-role-background-warning: oklch(0.5 var(--itx-accent-chroma) var(--itx-warning-hue));\n\t--itx-role-background-warning-subtle: light-dark(\n\t\toklch(0.89 var(--itx-accent-chroma-subtle) var(--itx-warning-hue)),\n\t\toklch(0.28 var(--itx-accent-chroma-subtle) var(--itx-warning-hue))\n\t);\n}";

export const ENGINE_CSS = "/*\n * Layer engine — how deep a thing is, and what that makes it. Hand-authored.\n *\n * ONE axis lives here: elevation.\n *\n *   --itx-surface and --itx-surface-above — the neutral substrate,\n *   backgrounds only.\n *\n * The four surface VALUES live in themes/…/ladder.css. This file holds no\n * colour: it counts depth and indexes them, and the number IS the depth.\n * Layer N is surface-N; -above is surface-(N+1), the one the next layer down\n * would paint, clamped at the deepest value.\n *\n * There is one page and it is layer 0. Nothing sits below it, which is why\n * there is no -below: a layer and a sink at the same depth are the same\n * colour — light from above is what separates them, not the substrate — so\n * the token said a direction its value did not have. A recess declares\n * itx-sink and reads --itx-surface.\n *\n * Everything drawn ON a surface is a role from themes/…/ladder.css. Roles are\n * absolute, so nothing here has to re-derive them per layer.\n *\n * ── Why this file looks the way it does ─────────────────────────────────\n *\n * An inherited custom property CANNOT increment itself. `--x: calc(var(--x)\n * + 1)` is a self-cycle, and a chain of slots re-declared in one block all\n * resolve against the SAME element, so it collapses to whichever slot the\n * block did not redeclare. The previous sliding-window implementation did\n * exactly that and never worked; see elevation-legacy.spec.ts.\n *\n * @container style() is the way out: a container query is evaluated against\n * the nearest ANCESTOR container, never the element itself, so there is no\n * cycle. Every element is a style container by default, so no container-type\n * is needed, and because --itx-layer inherits it compounds correctly through\n * arbitrary intermediate DOM.\n *\n * Three tiers, in source order — everything is :where() zero-specificity and\n * @container adds none, so later simply wins:\n *\n *   1. Layer 0, unconditional. NOT inside @container: if [interop-root] is\n *      <html> it has no ancestor container and the query is never known.\n *   2. A one-step floor for [itx-layer] / [itx-sink]. Browsers without style\n *      queries stop here — one step, not compounding, which is a defensible\n *      degradation rather than a broken page.\n *   3. The counter, unrolled. Overrides tier 2 wherever it is supported.\n *\n * Adding a layer means adding a @container block AND an absolute pin. That is\n * the one part of the colour system that cannot be a custom property, and\n * scripts/generate-globals.mjs signposts it for exactly that reason.\n *\n * Absolute pins come last so they outrank the counter — a dialog must not\n * inherit its depth from wherever it happens to sit in the DOM.\n *\n * If toggle() ever ships, tiers 2 and 3 collapse to one declaration:\n *   --itx-layer: toggle(0, 1, 2, 3, 4);\n */\n\n@property --itx-layer {\n\tsyntax: \"<integer>\";\n\tinherits: true;\n\tinitial-value: 0;\n}\n\n/*\n * Registered as <color> deliberately. An unregistered custom property that\n * goes invalid becomes the guaranteed-invalid value, which INHERITS and takes\n * the whole subtree down with it. Registered, a broken layer degrades to the\n * parent surface instead — and surfaces become transition-able between layers.\n */\n@property --itx-surface {\n\tsyntax: \"<color>\";\n\tinherits: true;\n\tinitial-value: transparent;\n}\n\n/* ── Tier 1 — layer 0, unconditional ─────────────────────────────────── */\n\n:where([interop-root], [itx-layer], [itx-sink]) {\n\t--itx-surface: var(--itx-surface-0);\n\t--itx-surface-above: var(--itx-surface-1);\n}\n\n:where([interop-root]) {\n\t--itx-layer: 0;\n\n\tbackground-color: var(--itx-surface);\n\tcolor: var(--itx-role-text);\n\n\t/* ── Z-index scale ───────────────────────────────────────────────── */\n\t--itx-z-dropdown: 1000;\n\t--itx-z-sticky: 1020;\n\t--itx-z-fixed: 1030;\n}\n\n/* ── Tier 2 — progressive-enhancement floor (no style queries) ───────── */\n\n:where([itx-layer]) {\n\t--itx-layer: 1;\n\t--itx-surface: var(--itx-surface-1);\n\t--itx-surface-above: var(--itx-surface-2);\n}\n\n:where([itx-sink]) {\n\t--itx-layer: 1;\n\t--itx-surface: var(--itx-surface-1);\n\t--itx-surface-above: var(--itx-surface-2);\n}\n\n/* ── Tier 3 — the counter. Strictly monotone up the tree, so never a cycle. */\n\n@container style(--itx-layer: 0) {\n\t:where([itx-layer]) {\n\t\t--itx-layer: 1;\n\t\t--itx-surface: var(--itx-surface-1);\n\t\t--itx-surface-above: var(--itx-surface-2);\n\t}\n\n\t:where([itx-sink]) {\n\t\t--itx-layer: 1;\n\t\t--itx-surface: var(--itx-surface-1);\n\t\t--itx-surface-above: var(--itx-surface-2);\n\t}\n}\n\n@container style(--itx-layer: 1) {\n\t:where([itx-layer]) {\n\t\t--itx-layer: 2;\n\t\t--itx-surface: var(--itx-surface-2);\n\t\t--itx-surface-above: var(--itx-surface-3);\n\t}\n\n\t:where([itx-sink]) {\n\t\t--itx-layer: 2;\n\t\t--itx-surface: var(--itx-surface-2);\n\t\t--itx-surface-above: var(--itx-surface-3);\n\t}\n}\n\n@container style(--itx-layer: 2) {\n\t:where([itx-layer]) { /* terminal — re-asserts without moving */\n\t\t--itx-layer: 2;\n\t\t--itx-surface: var(--itx-surface-2);\n\t\t--itx-surface-above: var(--itx-surface-3);\n\t}\n\n\t:where([itx-sink]) { /* terminal — re-asserts without moving */\n\t\t--itx-layer: 2;\n\t\t--itx-surface: var(--itx-surface-2);\n\t\t--itx-surface-above: var(--itx-surface-3);\n\t}\n}\n\n/* ── Absolute pins — last, so they outrank the counter ───────────────── */\n\n:where([itx-layer=\"0\"]) {\n\t--itx-layer: 0;\n\t--itx-surface: var(--itx-surface-0);\n\t--itx-surface-above: var(--itx-surface-1);\n}\n\n:where([itx-layer=\"1\"]) {\n\t--itx-layer: 1;\n\t--itx-surface: var(--itx-surface-1);\n\t--itx-surface-above: var(--itx-surface-2);\n}\n\n:where([itx-layer=\"2\"]) {\n\t--itx-layer: 2;\n\t--itx-surface: var(--itx-surface-2);\n\t--itx-surface-above: var(--itx-surface-3);\n}\n\n/* ── Paint. Zero specificity, so any consumer rule wins on contact. ──── */\n\n:where([itx-layer], [itx-sink]) {\n\tbackground-color: var(--itx-surface);\n\tcolor: var(--itx-role-text);\n}\n\n/*\n * Direction. Tone already said how FAR from the page a surface is; these\n * say which way, which is the half of the model tone used to carry.\n *\n * A layer and a sink at the same depth are the same colour on purpose —\n * light from above is what separates them, so a raise casts a shadow and a\n * recess is shadowed within. Zero specificity, so a component that paints\n * its own box-shadow wins on contact and nothing here needs unwinding.\n */\n:where([itx-layer]) {\n\tbox-shadow: var(--itx-elevation-shadow);\n}\n\n:where([itx-sink]) {\n\tbox-shadow: var(--itx-recess-shadow);\n}\n";
